# GEMINI_API_KEY: Optional for local development.
# When omitted, the server uses simulated audit reports so the site still runs.
GEMINI_API_KEY=""

# APP_URL: Optional public/local URL for self-referential links, callbacks,
# and API endpoints if future integrations need it.
APP_URL="http://localhost:3000"
